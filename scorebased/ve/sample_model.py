from score_models import ScoreModel, EnergyModel, NCSNpp, MLP, DDPM

def main():
     checkpoints_directory
     score = ScoreModel(checkpoints_directory=checkpoints_directory)
     sample = model.sample(shape=[1, 1, 64, 64], steps=1000)

if name == "__main__":
     main()
     
